using System;

namespace ConsoleApp2
{
    class Program
    { 
        static readonly int age; //动态常量可以一开始声明的时候不赋值，但一旦赋值，变不可修改，而且动态常量属于类字段，所以一定要写在class的括号里
        static void Main(string[] args)
        {
            
        }
    }
}